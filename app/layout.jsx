export const metadata = {
  title: 'TransferPath',
  description: 'Upload, verify, and evaluate transfer transcripts for faster registration decisions.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
